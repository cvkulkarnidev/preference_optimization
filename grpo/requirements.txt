# CUDA 12.2-compatible setup
# Install PyTorch first, from the same environment used for training:
# pip uninstall -y torch torchvision torchaudio trl transformers accelerate peft
# pip install torch==2.5.1 torchvision==0.20.1 torchaudio==2.5.1 --index-url https://download.pytorch.org/whl/cu121
# pip install -r grpo/requirements.txt

# Keep these pinned. Newer TRL/Accelerate builds may import FSDPModule,
# which is not available in some torch builds and causes import-time failures.
transformers==4.45.2
datasets==2.20.0
accelerate==0.34.2
trl==0.11.4
peft==0.13.2
tensorboard==2.17.1
scikit-learn==1.5.2
bitsandbytes==0.44.1
